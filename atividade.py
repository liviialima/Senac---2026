def calculos(a,b,c):
  notas =[]
  try:
    for in ranger(1,4):
    nota = float(input(f" digite sua nota{i}:")
    notas,append(nota)
mrdia = sum(notas) / len(notas)
 print(f" a nota final é: {media:.2f}"

  exept ValueError:
    print("digite somente numeros")

    calculos()
